pipeline {
    agent any

    stages {
        stage('Starting') {
            steps {
                echo 'Starting script /// Get ready'
            }
        }
        stage('Run') {
            steps {
                echo 'Running script /// Now'
            }
        }
    }
}
